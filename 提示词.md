你是一个精通深度学习、图像处理和 Web 前端开发的资深 Python 架构师。请根据以下结构化的算法逻辑与技术规范，编写一套完整的图像转黑白线稿系统代码。

系统由四个核心文件组成，请严格按照模块化设计，提供每个文件的完整、可运行代码。

---

### 通用技术栈要求
- **深度学习框架**: PyTorch (torch, torchvision)
- **图像处理库**: Pillow (PIL), NumPy, OpenCV (cv2)
- **Web 框架**: Gradio (Gradio >= 4.0 语法)

---

## 模块一：data.py (数据处理核心)

实现以下核心函数，严格遵循 I/O 规范：

### 1. `read_img_path(img_path)`
- **功能**: 
  - 使用 PIL 读取图片。
  - 记录原始图片尺寸 `(width, height)` 作为变量 `aus_resize`。
  - 强制转换图像色彩空间为 `'RGB'` 三通道模式（消除 Alpha 或 CMYK 通道）。
- **返回值**: PIL Image 对象, `aus_resize` 元组。

### 2. `get_transform(target_size=512)`
- **功能**: 构建一个 torchvision.transforms 组合。
  - 使用 **双三次插值（Bicubic Interpolation）** 将图像 Resize 到 `(target_size, target_size)`。
  - `ToTensor()`: 转换至 PyTorch 张量，数值空间自动缩放至 $[0.0, 1.0]$, 形状变为 `[C, H, W]`。
  - `Normalize()`: 使用均值 `0.5` 和标准差 `0.5`，通过公式 $Output = \frac{Input - 0.5}{0.5}$ 将像素值线性映射到 $[-1.0, 1.0]$ 区间。
- **返回值**: torchvision.transforms 对象。

### 3. `tensor_to_img(image_tensor)`
- **功能**: 逆向映射网络输出张量。
  - 去批次：提取 `image_tensor[0]` 将 4 维张量降至 3 维 `[C, H, W]`。
  - 逆归一化：通过公式 $(Input + 1.0) / 2.0 \times 255.0$ 将 $[-1.0, 1.0]$ 映射回 $[0.0, 255.0]$。
  - 转换为 NumPy 矩阵，并强制类型转换为 `np.uint8`。
  - 通道广播：如果通道数 $C == 1$，使用 `np.tile` 在通道轴复制 3 次，将其转换为标准 3 通道 RGB 格式。
  - 轴转置：使用 `np.transpose` 将 `[C, H, W]` 排列置换为符合 NumPy/PIL 规范的 `[H, W, C]`。
- **返回值**: `[H, W, C]` 的 NumPy 矩阵。

### 4. `save_image(numpy_img, aus_resize, save_path=None)`
- **功能**: 
  - 将 NumPy 矩阵通过 `Image.fromarray()` 封装为 PIL 对象。
  - **无损回弹**: 使用 **反向双三次插值（Reverse Bicubic Resampling）** 将图像拉伸放大恢复至 `aus_resize` 记录的原始尺寸。
  - 如果提供了 `save_path`，则保存文件。
- **返回值**: 恢复原始分辨率后的 PIL Image 对象。

---

## 模块二：model.py (神经网络架构)

实现线稿提取核心网络，包含两种模式：

### 1. `UnetGenerator` (default 模式架构)
- **输入**: `[B, 3, H, W]`，**输出**: `[B, 1, H, W]`（硬编码单通道输出，实现色彩剥离）。
- **架构**: 8 层下采样（Encoder）与 8 层上采样（Decoder）组成的 U-Net。
- **编码器**: 使用步长为 2 的步进卷积（`nn.Conv2d(..., stride=2)`)。
- **跳跃连接**: 在解码器每层上采样时，使用 `torch.cat` 将编码器对应层的底层几何特征在通道维度拼接。
- **最后一层**: 激活函数必须为 `nn.Tanh()`。

### 2. `ImprovedUpsampleSmooth` (improved 模式架构)
- **功能**: 用于替换传统转置卷积以消除棋盘状伪影（Checkerboard Artifacts）。
- **平滑上采样模块**:
  - 先使用 `F.interpolate(..., mode='bilinear', align_corners=False)` 将特征图放大 2 倍。
  - 紧接着通过一个固定 $3 \times 3$ 大小的高斯平滑滤波器（通过 `F.conv2d` 实现，内核权重初始化为二维高斯分布，中间高四周低）。
- **残差特征微调 (MLP 块)**:
  - 经过平滑的特征图输入一个由两层 $1 \times 1$ 卷积与 `nn.GELU()` 激活函数组成的 MLP 块进行非线性变换。
  - 使用残差连接（`output = mlp(x) + x`）补充特征。

---

## 模块三：test.py (推理控制与特征增强)

### 1. `equalize_clahe(img_np, clip_limit=2.0)`
- **功能**: 局部对比度自适应直方图均衡化。
  - 输入 PIL 图像或 NumPy 矩阵，转换为 OpenCV 格式。
  - 使用 `cv2.createCLAHE(clipLimit=clip_limit, tileGridSize=(8,8))` 创建自适应直方图平衡器，对图像进行边缘反差拉伸，防止暗部线条丢失。
- **返回值**: 处理后的 NumPy 矩阵。

### 2. `sketch2anime(img_obj, mode='default', use_clahe=False, clahe_clip=2.0)`
- **核心推理流水线**:
  1. 接收前端传入的图像对象（路径或PIL实例）。
  2. 调用 `data.read_img_path` 解码并捕获原始尺寸 `aus_resize`。
  3. （可选）如果 `use_clahe=True`，触发 `equalize_clahe` 增强局部对比度。
  4. 调用 `data.get_transform()` 对图像进行下采样、张量化和 `[-1.0, 1.0]` 归一化。
  5. **维度升维**: 使用 `image.unsqueeze(0)` 在第 0 维强行增加批次维度，使其从 `[C, H, W]` 变为 `[1, C, H, W]`。
  6. 送入 `create_model(mode)` 实例，在 `torch.no_grad()` 模式下进行前向传播，得到单通道输出张量。
  7. 调用 `data.tensor_to_img` 进行去批次、逆归一化、通道广播和轴转置。
  8. 调用 `data.save_image` 依照 `aus_resize` 进行分辨率无损回弹。
- **返回值**: 最终的高质量线稿 PIL Image 对象。

---

## 模块四：gradiodemo.py (前端交互生命周期)

使用 **Gradio >= 4.0** 的组件构建 Web 交互界面：
- **界面布局**: 
  - 输入端: 包含一个图片上传拖拽区 (`gr.Image(type="pil", label="上传彩色照片")`)。
  - 参数控制区: 
    - 模式选择单选框 (`gr.Radio(choices=["default", "improved"], value="default", label="提取模式")`)。
    - CLAHE 增强开关 (`gr.Checkbox(label="开启局部对比度自适应均衡(CLAHE)")`)。
    - CLAHE 阈值滑块 (`gr.Slider(minimum=1.0, maximum=5.0, value=2.0, label="对比度限制系数")`)。
  - 输出端: 结果展示区 (`gr.Image(type="pil", label="高质量黑白线稿")`)。
- **异步数据流捕获**:
  - 当用户上传照片或调整参数时，绑定上述组件并将它们作为参数异步传递给后端的推理核心函数 `sketch2anime`。
  - 获取生成的无损分辨率 PIL 对象后，由 Gradio 底层自动编码为内存图像流，实时渲染到用户的浏览器屏幕上。

---

### 编码规范约束
1. 代码必须包含清晰的注释，指出每个步骤对应哪一个算法阶段。
2. 神经网络部分请提供完整的 Dummy 权重初始化或简化版的前向传播代码，确保整个数据流可以完整 Mock 通畅运行。
3. 严格处理张量维度的转换（如 `unsqueeze`, `transpose`），避免运行时发生 Shape Mismatch 错误。


---
不要修改已经写好的代码，不要修改线稿提取功能
请帮我在用户上传图片时，添加一个【图片裁剪功能】。具体需求如下：

### 1. 核心功能与比例限制
- 用户选择/上传图片后，触发图片裁剪弹窗或组件。
- 裁剪框的比例必须**强制固定为正方形（1:1 比例）**，不允许用户自由拉伸成非正方形。

### 2. UI 界面与遮罩样式
- **裁剪区域（选中区）**：保持图片原本的亮度和清晰度，不做任何遮罩处理。
- **非裁剪区域（未选中区）**：需要覆盖一层半透明的黑色阴影遮罩（例如 `rgba(0, 0, 0, 0.5)`），以突出显示裁剪框内的内容。
- 裁剪框的四周需要有明显的边框或边角标识（参考主流图片裁剪器的白色直角边框）。

### 3. 交互与输出
- 用户可以拖动裁剪框移动位置，或者缩放裁剪框（但缩放时必须保持 1:1 比例）。
- 提供“确认裁剪”和“取消”按钮。
- 点击“确认”后，输出裁剪后的图片数据（可以是 Base64、Blob 或 File 对象），用于后续的上传或保存。

### 4. 技术栈建议
- 请结合我们现有的项目技术栈（如果是 React 建议使用 react-image-crop，Vue 建议使用 vue-cropper，原生 JS 建议使用 cropperjs），并直接修改或新建对应的组件文件。